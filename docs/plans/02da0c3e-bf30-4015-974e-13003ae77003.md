## Complexity Assessment

Complexity: Medium
Rationale: Automatically assessed for compatibility.

## Goal
Redesign the CyOps Cookbook web app from a text-heavy, complex UI to a visually-driven, minimal interface: full-viewport splash image with shatter animation on entry, followed by a simple home page showing live vote count + "Gen me an idea" CTA, and clear shared-credit-pool messaging (1000 total across all users).

## Context
- **Stack**: React 18, TypeScript, Vite, Tailwind CSS, React Router v6
- **Theme**: Dark cyberpunk (`#0A0E17` bg, cyan/pink/purple neon accents)
- **Current landing page** (`GuidePage.tsx`): heavy three-panel dashboard (rules, scoring rubric, track cards), hero text, stats bar — user finds it too complex
- **Splash screen** already exists (`SplashScreen.tsx`) with a working 2s display → 6×4 grid shatter animation. Currently loads `splash-cover.png` from assets.
- **Ideas page** (`IdeasPage.tsx`): entry screen with Quick Generate + Chat Quiz options, streaming agent chat — already functional, left mostly untouched
- **Vote page** (`VoteForMePage.tsx`): vote chart + live feed — already functional, left mostly untouched
- **Credits system**: global pool (1000 total, shared across all users), 100 per vote, 20 per AI call, backed by `/api/credits` and `/api/votes`
- **Specified splash image**: `docs/requirements/64f6aa2425a2-image.png` (the user's desired opening image)
- **Project path**: `/home/harness/cyops_data/workspace/cyopsarenacookbook/`
- **Web app root**: `apps/web/src/`

## Assumptions
- The backend API (`/api/credits`, `/api/votes`, `/api/ideas/quick`, `/api/ideas/chat-stream`, `/api/prompt/start`, `/api/prompt/answer`) is already working and will not change.
- The splash shatter animation logic in `SplashScreen.tsx` is working correctly; the only change is replacing the image asset used.
- The existing `CreditsBadge` component in the `TopBar` is retained for header display.
- The existing `IdeasPage` and `VoteForMePage` do not need structural changes — only the home page (`GuidePage`) and splash image are in scope.
- The user does not want to remove the Ideas/Vote pages, only to make the entry/home page drastically simpler.
- The TopBar (logo, nav, credits badge, language dropdown) is acceptable to keep as-is.

## Acceptance Criteria
- AC-1: Splash screen shows `64f6aa2425a2-image.png` full-viewport for 2 seconds, then shatters into a 6×4 fragment grid animation, then reveals the app.
  - Copy `docs/requirements/64f6aa2425a2-image.png` into `apps/web/src/assets/` (keep `splash-cover.png` for rollback).
  - Update the import in `SplashScreen.tsx` to use the new image.
  - Verify: open the app fresh; see the new image fullscreen, wait 2s, observe shatter, see main page fade in.

- AC-2: Home page (`/`) is visually minimal and image-forward — a centered hero with live vote count, shared credit pool status, and a single "Gen me an idea" CTA button.
  - Replace the entire content of `GuidePage.tsx` with a clean, centered layout.
  - Show live vote count (large, green neon, polling + SSE). Label: "Arena Votes".
  - Show shared credit pool balance with a sub-label making it clear: "1000 credits shared by all participants".
  - Show one prominent CTA button: "⚡ Gen me an idea" — links to `/ideas` or triggers quick-generate directly.
  - No three-panel dashboard. No rules/scoring/tracks text blocks.
  - Add minimal, large-scale visual flourishes (e.g. subtle background image or gradient, cyber grid kept, scanlines kept).
  - Sub-files: `apps/web/src/pages/GuidePage.tsx`, `apps/web/src/i18n/en.json` (update/add landing keys), `apps/web/src/i18n/zh.json` (add zh equivalents).

- AC-3: The shared-credit-pool concept is unambiguous in the UI.
  - Credit display text reads "Shared Pool" with a sub-label like "1000 credits shared by all participants" or the current `landing.poolHint`.
  - If the pool is paused/blocked, show that state clearly.
  - Sub-files: same as AC-2.

- AC-4: Existing routes `/ideas` and `/vote` continue to work without regression.
  - `IdeasPage` and `VoteForMePage` are not structurally edited beyond any necessary import/export adjustments.
  - TopBar navigation between pages remains functional.

- AC-5: The splash screen asset change is clean and the original asset is preserved.
  - New image at `apps/web/src/assets/splash-hero.png` (or similar name).
  - Old `splash-cover.png` remains in place for rollback.
  - `SplashScreen.tsx` import updated.

## Verification Matrix
| AC | Files or Areas | Verification Command or Check | Success Signal |
|---|---|---|---|
| AC-1 | `apps/web/src/assets/` (new image), `apps/web/src/components/SplashScreen.tsx` | `cd apps/web && npx vite build` (no errors), then manually open dev server at `http://localhost:5173` | New image displays full-viewport, then shatters after 2s into 6×4 grid fragments |
| AC-2 | `apps/web/src/pages/GuidePage.tsx` | Navigate to `/` after splash; observe centered layout with vote count, credits, CTA button | Page shows only: large vote number, credit pool status, "Gen me an idea" button — no three-panel dashboard, no dense text |
| AC-3 | `apps/web/src/pages/GuidePage.tsx`, `apps/web/src/i18n/en.json` | Check the credit pool label and sub-label on the home page | Text communicates "shared pool" and "1000 credits shared by all participants" |
| AC-4 | `apps/web/src/pages/IdeasPage.tsx`, `apps/web/src/pages/VoteForMePage.tsx` | Click through nav to `/ideas` and `/vote` | Both pages render full content with no console errors |
| AC-5 | `apps/web/src/assets/`, `apps/web/src/components/SplashScreen.tsx` | `ls apps/web/src/assets/` shows both `splash-cover.png` and the new image file; import in SplashScreen references new file | Old asset preserved, new asset used |

## Implementation Notes
1. **Copy the splash image**: `cp docs/requirements/64f6aa2425a2-image.png apps/web/src/assets/splash-hero.png`
2. **Update SplashScreen.tsx**: Change `import splashCover from "../assets/splash-cover.png"` to `import splashCover from "../assets/splash-hero.png"`. No other changes needed — the existing animation logic is correct.
3. **Rewrite GuidePage.tsx** (home page): Remove the three-panel dashboard entirely. Keep the vote/credits polling logic. Render a minimal, centered layout:
   - A large background area with the cyber grid and scanlines (from `App.tsx`)
   - Central column: large vote counter (like current stats bar but bigger), credit pool status, single CTA button
   - The CTA can either navigate to `/ideas` or trigger `api.quickIdea()` directly (navigating is simpler and keeps separation of concerns)
4. **Update i18n keys**: Ensure `landing.votesLabel`, `landing.poolHint`, `landing.cta`, `credits.poolLabel`, `credits.poolBlocked` keys exist and are correct in both `en.json` and `zh.json`. Add any new keys needed for the simplified home page.
5. **Do NOT touch**: `SplashScreen.tsx` animation logic, `IdeasPage.tsx`, `VoteForMePage.tsx`, `TopBar.tsx`, `CreditsBadge.tsx`, `App.tsx` routing structure, `styles.css` (except for minor new animation/keyframe additions if needed), backend APIs, database schema, `vite.config.ts`, `tailwind.config.js`.
6. **Verification**: After building, run `npx vite` in `apps/web/` and visually confirm all acceptance criteria.

## Out of Scope
- Changes to backend APIs, database, or vote/credit logic
- Changes to IdeasPage UX flow (Quick Generate / Chat Quiz)
- Changes to VoteForMePage UX
- Removing any existing pages or routes
- Adding new routes
- Mobile-responsive design overhaul (current responsive classes preserved where possible)
- Adding new animation libraries or dependencies
- Changing the cyberpunk theme or color palette